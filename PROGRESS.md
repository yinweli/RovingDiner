# 工作進度與接續記錄

本檔是跨 session 的接續工作交接點。每次工作告一段落時更新「里程碑進度」與「接續待辦」,下次開新 session 先讀本檔即可接手。

紀律:本檔只記「從 git 歷史與 `doc/` 規格看不出來的待辦與決策」。規則細節以 `doc/` 規格為準、程式現況以 git 為準,不在此重複;站內拍板一經成文於規格即自本檔刪除(過程細節 git log 可查;2026-06-11 大砍前的歷史拍板全文見 git 歷史)。

## 現況

- **架構已定案**(實作規格書 §一~§四):核心四包依賴 `games → rules → cores → exprs` 一條直線,`internal/infra` 基礎設施、`internal/tester` 跨包測試基建;TUI = `app/rodi`(停點直讀 + 交棒 stepper)+ `cmd/rodi` 瘦進入點。
- **M0–M25 已落地,M26(互動與 modal)進行中**:核心線(M0–M16)→ 事件流收口(M17–M18)→ TUI 被動觀看(M19–M22)→ 暫停機重構(M23)→ 日誌流換軌(M24)→ 速率(M25)。M26 子切 R1/R1.5/R2/R3 已落地(三模式框架+切區聚焦/框線網格+日誌 flex/游標+捲動/modal 框架+計數 modal),剩 R4 四張檢視 modal(子切與拍板見接續待辦)。建置 / golangci-lint(0 issues)/ 測試全綠,cores 與 rules 覆蓋率 100%(app/rodi 除 TTY 組裝入口 Run 外 100%)。
- **剩餘站序已凍結**(2026-06-11 拍板):M26 互動與 modal → M27 選取＋Operator → M28 企劃驗證器 → M29 conformance golden;細目以實作規格書【九】為準,再插站不改號。

## 里程碑進度

對應 `doc/營業實作規格書.md`【九、里程碑】(細節以該處為準)。

| 里程碑  | 狀態 | 說明                                       |
|:--------|:-----|:-------------------------------------------|
| M0–M16  | ✅   | 核心線(exprs/cores/rules/games,跑通第一局) |
| M17–M18 | ✅   | 事件流收口(合約+全引擎發射點)              |
| M19–M22 | ✅   | TUI 被動觀看(殼/渲染地基/六區/事件日誌)    |
| M23     | ✅   | 暫停機重構(交棒 stepper+盤面直讀)          |
| M24     | ✅   | 日誌流換軌(行組 Emit+發射台+golden 常駐)   |
| M25     | ✅   | 速率(快/慢/步進排拍+世代驗章)              |
| M26     | ⬜   | 互動與 modal *(prov)*                      |
| M27     | ⬜   | 選取＋Operator *(prov)*                    |
| M28     | ⬜   | 企劃驗證器 *(prov)*                        |
| M29     | ⬜   | conformance golden(擴充覆蓋) *(prov)*      |

## 接續待辦

- **clamp 範圍只做規格明寫者**:屬性修改僅 護盾 / 格擋 夾下限 0(`Value.Clamp`);其餘(morale 對 moraleMax 上限、sate / calm 下限等)規格未明寫,不臆測,跑流程時補。
- **規格明文回補(M9 旗標確認)**:① morale `-=` 開頭先 `Round(N)` 為整數扣減值;② `cardCost*` 尊重 cost 鎖定;③ `guestSeat` / `guestReturn` / `restore` 入座取隨機空位——三者已實作但規格【十七】/【二十五】條文未明寫,宜回頭補規格明文。
- **[M26 進行中] 子切序 R1 → R4(R1.5 插站)**:R1 三模式框架+切區聚焦(✅)→ R1.5 框線補齊+日誌寬度 flex(✅;§6.1 完整框線網格是 M19–M22 未竟項,非拍板省略)→ R2 游標+捲動(✅;component 介面長 `View(game, width, focus)` + `Move(game, key)`、cursor-follow 窗格起點純由游標導出不另存狀態、`Item(game)` 延後到 R4 由 modal 消費者驅動)→ R3 modal 框架+計數 modal(✅;modal 介面 `Body(game)`、分隔線以 `+-` 前綴行表達、開 modal 世代+1 暫停消費、Enter 進場但僅狀態列有作用且不列示——`[Enter]檢視` 標籤隨 R4 點亮;cores 補唯讀 `Tally.Group()` 供樞紐表列舉)→ R4 四張檢視 modal(顧客/行動/效果/卡牌,Enter 對游標項目;含 `Item` 介面)。
- **[M26 已拍板] 日誌寬度 flex(R1.5 已實作、收站改 §8)**:日誌寬 = clamp(總寬-70, 30, 50)——100 欄最低尺寸維持 30(左欄 70 的 2 桌硬需求不破),餘寬先給日誌到 50 上限、之後才給左欄;代價 = §8「~120 欄可視 3 桌」註記後移(~135 欄),用戶拍板日誌優先。
- **[M26 已拍板] 框線網格(R1.5 已實作、收站視需要成文)**:中線歸左欄(面板每行自畫完整列框,標題行收尾 `+`/內容行收尾 `|` 即共用中線;日誌不畫左框、用接縫版標題與列)、內容寬 = 區寬-4(框+padding 各 1)、狀態列補標題列、底框列歸父層、gap 墊高行帶框(格線不開洞)、聚焦高亮維持「標題列反白」不做全框換色(共用線結構上做不乾淨,2026-06-11 與用戶確認)。
- **[M26 已拍板](收站成文於規格後自本檔刪)**:
  - **游標自持形態**:組件指標化+能力介面(`Move(game, key)` + `Item(game)`,日誌 Move=viewport 捲動/Item=nil、狀態列 Move=no-op/Item=計數依據);游標存 index、讀取時夾界(盤面拍間會變,指標當游標會懸空);`View` 加 focus 參數(游標態只在聚焦區顯示);細部簽章 R2 開工再定。
  - **聚焦「邊框高亮」= 首行反白**(面板標題列/狀態列標籤行):不補滿框、不動行數預算;聚焦屬區層級歸父層上色,游標態屬項目層級歸組件(R1 已實作,收站成文於顯示規格書 §7.1)。
  - **8 區循環序 = §7.1 列序**(座位→場外→行動→效果→手牌→牌堆→狀態列→日誌),起始聚焦=座位、恆有聚焦區(不設無聚焦態)。
  - **modal 開著時暫停消費**:開 modal 世代+1 斷在途拍、tick 以 modal 堆疊非空門控(比照步進擋法);速率 `mode` 欄位不動(modal 是疊加態),關閉依當前模式重排拍;[N]/[Space] 由 modal 態鍵表天然失效(R3 實作)。
- **[M27 開站拍板項] 交棒廣義化 + Operator 簽章補請求語境**:`Next` 回傳廣義化為 tagged 輪次(行組 | 輸入請求 | 終局,單一 channel;request 自帶 answer channel);`Operator` 簽章補請求語境參數(選取提示 `<來源名> 要求選<對象>` 的來源文字,發射點本就算得、順手下傳;cores 介面變更、全 Fake 替身連動,列第一子切)。`PlayerAction` 等待 = 常態 UI 態(P = 游標手牌 / E = 結束)、`Pick*` 等待 = 選取模式。候選身分 = 實例指標(直讀架構下 Operator source 與面板渲染同批指標,指標相等即可、零 ID 對映)。
- **[企劃驗證器 M28] 命令對象參數數量 / 型別校驗**:M8 selector 對 `[...]` 參數採執行期寬鬆(數量 / 型別不符 → 空集合 no-op),未做 Validate 期 arity 檢查(裸寫 `guestPick`、`handAll` 缺卡牌編號等於 Validate 漏過、執行期靜默 no-op);靜態 arity 校驗歸 M28。**開站先拍詞彙 metadata 形狀**:map 值升級為小 struct(具名函式 + arity 欄,單一定義點不漂移)vs 另立表(傾向前者;每詞條一具名函式不變,屬新議題、與 rules 盤點否決不衝突)。
- **[企劃驗證器 M28] 引用左值的引用基底合法性**:`Validate` 對引用左值只查屬性可寫性(`HasAttrRefWrite`),未查引用基底是否為合法物件引用——`morale.cost = 1` 漏過(執行期安全 no-op);需補匯出述詞(如 `HasObjectRef`)。
- **[企劃驗證器 M28] 其餘**:門檻配對(`門檻值^技能編號`)的嚴格驗證要與 cores 寬鬆解析共用單一格式來源(抽共用 parse,勿雙寫格式知識);緩議:適用類型矩陣要不要驗(如立即類型填了 TriggerKind 報不報),開站時拍板。
- **[緩議] -race 未跑**:無 gcc 環境;嚴格交棒按構造無並行存取,雙跑同序測試為行為釘。

## 已敲定的設計決策(勿重新爭論;只列規格與程式看不出來的)

- **牌堆順序 = slice 前端為頂端**(index 0、最新進入者):`deckTop` / `dropTop` 取 `slice[:N]`、新進入者 prepend、auto-shuffle 洗後棄牌 append 至尾端(底部)。規格只定「新進入者置頂」未綁 slice 哪端;M8 起多站依賴,已鎖定不再改向。
- **rules 盤點全數否決**:維持每詞條一具名函式;詞條工廠化 / 合併 / 預建索引等十項盤點建議全數不採、不再重提。
- **C#/Unity 移植需求已撤銷**:架構不受可攜性約束,Go 慣用法自由採用。
- **規格未明處以實作現況為準**(exprs 比較 / 相等 / Truthy 語意、寫入語意走 Value 守衛方法等):規格補強時再對齊,不反向遷就臆測。
